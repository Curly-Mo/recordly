<%= simple_form_for(@favorite_album) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :album %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
